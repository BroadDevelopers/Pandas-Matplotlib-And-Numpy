"""
Numpy is used in scientific computing
Numpy main object is n dimensional Arrays
"""

import numpy as np
import time
import sys

# creating an array ovject
# np.array([1,2,3])
# similar to a list
# why do we need numpy arrays
# benefits:
# 1Less memory
# 2Fast
# 3Convinient

# creating a list with thousand element
# printing the size of the list
#l = range(1000)
#print(sys.getsizeof(5) * len(1))
#14000bytes of memory

# arraysize=1000 total length of an array
# itemsize is item size-size of one element
#array = np.arange(1000)
#print(array.size * array.itemsize)
#4000bytes of memory

#numpy is fast and convinient
l1 = range(SIZE)
l2 = range(SIZE)

start = np.arange(SIZE)